{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "search",
  "version": "1.0.0",
  "displayName": "Jadlis — поиск и ключи ресерч-стека",
  "description": "Поиск в вебе (Brave — по словам, Exa — по смыслу) и страницы через Firecrawl, плюс пять MCP-серверов и единая точка ключей (scripts/secret.sh → Связка ключей macOS). База для плагинов research и science-research.",
  "author": {
    "name": "Be Cyborg"
  },
  "homepage": "https://github.com/beCyborg/jadlis-search",
  "repository": "https://github.com/beCyborg/jadlis-search",
  "keywords": [
    "jadlis",
    "search",
    "brave",
    "exa",
    "firecrawl",
    "mcp"
  ],
  "userConfig": {
    "BRAVE_API_KEY": {
      "type": "string",
      "title": "Brave Search API Key",
      "description": "Тариф Search. Завести: api-dashboard.search.brave.com. Значение уезжает в Связку ключей macOS; сменить позже — /plugin configure search@jadlis.",
      "sensitive": true,
      "required": true
    },
    "FIRECRAWL_API_KEY": {
      "type": "string",
      "title": "Firecrawl API Key",
      "description": "Формат fc-… Завести: firecrawl.dev/app/api-keys. Значение уезжает в Связку ключей macOS; сменить позже — /plugin configure search@jadlis.",
      "sensitive": true,
      "required": true
    },
    "REDDITAPIS_KEY": {
      "type": "string",
      "title": "RedditAPIs Key (резервный Reddit-MCP, опционально)",
      "description": "Ключ для запасного сервера reddit-alt (redditapis.com), ~$0.002 за вызов. Без ключа сервер поднимается, но отдаёт 401 — в /mcp он будет гореть красным; это ожидаемо, основной Reddit-канал и no-auth-лестница (Arctic Shift / Reddit search RSS) работают без него.",
      "sensitive": true,
      "required": false
    },
    "YOUTUBE_API_KEY": {
      "type": "string",
      "title": "YouTube Data API v3 Key (канал youtube, опционально)",
      "description": "Ключ Google Cloud (YouTube Data API v3), квота 10 000 units/день; поиск = 100 units/вызов, протокол капит ≤3 вызова за прогон. Без ключа сервер youtube отдаёт ошибку и в /mcp горит красным — это ожидаемо: канал youtube деградирует на Brave site:youtube.com + локальные транскрипты (scripts/yt-transcript.py) и работает.",
      "sensitive": true,
      "required": false
    }
  }
}
